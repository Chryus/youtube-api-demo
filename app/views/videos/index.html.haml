%h1 Welcome to videodrome
%br

%ul
- @videos.each do |video|
  %h4
    = "#{video.id}. ‘#{video.title}’"
  %h5
    = "by #{video.artist}"
  %h5
    = "year: #{video.year}"
  = button_to 'Edit', "videos/#{video.id}/edit"
  = button_to 'Delete', "videos/#{video.id}", method: :destroy, confirm: "Are you sure you want to delete this video?"

